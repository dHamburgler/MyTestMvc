using EurofinsEvents.Data;
using EurofinsEvents.Models;
using EurofinsEvents.Repositories.Interface;
/*using Microsoft.EntityFrameworkCore;


namespace EurofinsEvents.Repositories.Implemntation
{
    public class EventAttendeeService : IEventAttendeeService
    {
        private readonly ApplicationDbContext _dbContext;

        public EventAttendeeService(ApplicationDbContext dbContext)
        {
            _dbContext = dbContext;
        }

        public async Task AddAttendeeAsync(int eventId, string userId)
        {
            // Implement logic to add an attendee to the event
            await _eventAttendeeRepository.AddAttendeeAsync(eventId, userId);
        }

        public async Task<IEnumerable<EventAttendee>> GetAttendeesAsync(int eventId)
        {
            // Implement logic to retrieve attendees for the event
            return await _eventAttendeeRepository.GetAttendeesAsync(eventId);
        }

        // Other method implementations...
    }
}
*/
